server:
  port: 8082

spring:
  application:
    name: PostMS
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefixes: config
        default-context: application
        profile-separator: "::"
        format: YAML